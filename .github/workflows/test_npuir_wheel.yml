name: Test NPUIR Wheel

on:
  workflow_call:
    inputs:
      checkout-ref:
        description: "Git ref to checkout (e.g. refs/pull/123/merge or refs/tags/v1.0.0)"
        type: string
        required: true
      report-name:
        description: "Name for the test report artifact"
        type: string
        required: true
      wheel-artifact:
        description: "Name of the wheel artifact to download"
        type: string
        default: "tilelang-npuir-cpp-coverage-py3.11-arm64"

jobs:
  test:
    runs-on: linux-aarch64-a3-2
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:8.5.0-a3-ubuntu22.04-py3.11
    timeout-minutes: 360
    env:
      UV_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
      UV_EXTRA_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu https://repo.huaweicloud.com/ascend/repos/pypi"
      UV_INDEX_STRATEGY: "unsafe-best-match"
      UV_INSECURE_HOST: "cache-service.nginx-pypi-cache.svc.cluster.local"
      UV_HTTP_TIMEOUT: 120
      UV_NO_CACHE: 1
      UV_SYSTEM_PYTHON: 1

    steps:

    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: ${{ inputs.checkout-ref }}
        # The recent-code coverage report uses git blame to exclude lines older
        # than eight months, so it needs history beyond the checkout commit.
        fetch-depth: 0

    - name: Download ARM64 C++ coverage wheel
      uses: actions/download-artifact@v4
      with:
        name: ${{ inputs.wheel-artifact }}
        path: dist/

    - name: Install tilelang and test dependencies
      run: |
        CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
        sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
        pip config set global.index-url http://${CACHING_URL}/pypi/simple
        pip config set global.trusted-host "${CACHING_URL}"
        pip install uv
        uv pip install torch==2.7.1
        uv pip install torch-npu==2.7.1
        uv pip install dist/*.whl
        uv pip install pytest pytest-xdist pytest-html pytest-cov numpy

    - name: Install LLVM coverage tools
      run: |
        apt-get update && apt-get install -y clang llvm

    - name: Prepare npuir test workspace
      run: |
        mkdir -p testing/npuir/output
        if [ -d tilelang ] && [ ! -d _tilelang_src ]; then
          mv tilelang _tilelang_src
        fi

    - name: Run examples and npuir tests with Python and C++ coverage
      env:
        TILELANG_CLEAR_CACHE: "1"
        TILELANG_CPP_COVERAGE: "1"
        TILELANG_CPP_COVERAGE_REPO_ROOT: ${{ github.workspace }}
        TILELANG_CPP_COVERAGE_OBJECT_DIR: ${{ github.workspace }}/coverage/cpp-objects
        LLVM_PROFILE_FILE: ${{ github.workspace }}/coverage/cpp-profraw/%m-%p.profraw
      run: python scripts/run_python_coverage.py -v -n 2

    - name: Upload test report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.report-name }}
        path: |
          testing/npuir/output/
          coverage/
